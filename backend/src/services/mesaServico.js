const pool = require('../config/db')
const Mesa = requrie('../models/Mesa')

async function criarMesa({ codigo_mesa, status, capacidade_minima, capacidade_maxima, area_id }){
    const checkQuery = ''
}